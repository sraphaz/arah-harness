id: cli
name: CLI ARAH
mode: consult
enrich: |
    Entry point arah.ps1 — não quebrar compat PS 5.1.
validate: |
    self-test.ps1 cobre init + domain sync + export-graph.
autonomy:
  - consult_post
scope:
  paths:
    - cli/**

guardrails:
  no_merge: true
  consult_only: true
